{
  "name": "chat-app",
  "version": "1.0.0",
  "description": "Modern chat app",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "postinstall": "cd frontend && npm install && npm run build"
  },
  "dependencies": {
    "express": "^4.18.2",
    "socket.io": "^4.7.2"
  }
}
